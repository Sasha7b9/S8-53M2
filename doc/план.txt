- Маркер TShift при включении на медленной развёртке рисуется не не месте (смещён влево очень сильно)
- Для маркера TShift в окне памяти нужно брать TShift из SET_TSHIFT
- При включении ма медленной развёртке мусор на экране
- Нет усреднения рандомизатора в отладке
- Накопление "бесконечность" не работает
- Зависает на самописце
- Не работают маркеры автоматических измерений частоты и периода
- При переключении количества точек в рандомизаторе в окне памяти артефакты
- В поточечном режиме на развёртке 0.2с при вклюённом пиковом детекторе неправильно отрисовывается в окне памяти
- Не работает ДИСПЛЕЙ/НАСТРОЙКИ/ЦВЕТА. Вроце цвета изменяются, но видно это только после перевключения
- Ноль неправильно отрисовывается в раскрытом Governor
- В раскрытом ChoiceReg по нажатию F не происходит переключение
- При вращении смещения по времени в режиме СТОП нет задержки на нуле
- На достаточно медленной развёртке если если смещаем сигнал вверх или вниз то он прыгает
- Инверсия должна производиться не во время чтения, а непосредственно во время вывода
- Поточечные развёртки начинаются с 20мс/на деление, а период опроса флага - 1мс. Нужно сделать период опроса хотя бы 500мкс.
- При включении на однократной синхронизации поточечной развёртки на экране мусор.
- Отрисовка пикового детектора при сжатом по горизонтали экране
- Переключение количества точек не работает
- Нажатие кнопки УСТАНОВКА не закрывает меню
**** БАГИ ****
БАГ В обработчике кнопок панели при нажатии кнопки СЕРВИС происходят ложные события RShift, TShift. Пока навешен костыль в виде функции IgnoreEvent()
БАГ При рандомизаторе открыть ОТЛАДКА-РАНД-ТОР-УСТРЕДНЕНИЕ, частоту 11.15 МГц - при вращении усреднения виснет
БАГ Виснет на преключении ПАМЯТЬ-Точки
БАГ Приблизительное усреднение неправильно работает (на 2 работает похоже, что больше)
БАГ Плохо работает поиск сигнала
БАГ VCP должен подключаться без передёргивания шнура (сейчас если во время заливки прошивки или включения прибора шнур вставлен, VCP не работает).Пишут, что нужно дёргать ножкой D+, а не просто подтягивать к питанию, если хочешь не передёргивать провод.
**** Вторично ****
- Отрисовка сигнала пикового дектора в поточечном режиме
- В окне памяти неправильно выводится сигнал - вверху искажение при приближение к верху сетки
- В рандомизаторе без синхронизации неправильно рисуется сигнал
- После сброса настроек вылет в Storage::GetDataSettings
- Сигнал сдвинут на одну точку влево (хорошо видно на сигнале калибратора)
- Надписи малым шрифтом проверить (например, в меню КУРСОРЫ-УСТАНОВИТЬ)

*** Исправленные баги ***
18.02.22 Чёрный экран при нажатии/отпускании кнопки был вызван звуком

+-------+----------+
| Дата  | Сколько  |
|       | оставить |
+-------+----------+
| 22.03 |    27    | -14
| 23.03 |    13    |
| 24.03 |     0    |
|       |          |
|       |          |
|       |          |
+-------+----------+
